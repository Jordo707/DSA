/*

Implement pow(x, n), which calculates x raised to the power n (i.e., xn).

*/

const myPow = (x, n) => x ** n

// Test Cases
console.log(myPow(2, 10)); // 1024
console.log(myPow(2.1, 3)); // 9.261
console.log(myPow(2, -2)); // .25
